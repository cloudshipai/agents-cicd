<!DOCTYPE html>
<html>
<head>
    <title>Error <%= status %></title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        .error-container { max-width: 600px; margin: 0 auto; }
        .error-code { font-size: 3em; color: #e74c3c; margin-bottom: 20px; }
        .error-message { font-size: 1.2em; margin-bottom: 20px; }
        .stack { background: #f8f9fa; padding: 15px; border-radius: 5px; font-family: monospace; white-space: pre-wrap; }
        .back-link { margin-top: 20px; }
        .back-link a { color: #3498db; text-decoration: none; }
        .back-link a:hover { text-decoration: underline; }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-code"><%= status %></div>
        <div class="error-message"><%= error %></div>
        
        <% if (stack) { %>
        <div class="stack"><%= stack %></div>
        <% } %>
        
        <div class="back-link">
            <a href="/">← Back to Home</a>
        </div>
    </div>
</body>
</html>